<Page
    x:Class="GAMINGCONSOLEMODE.onboarding"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GAMINGCONSOLEMODE">

    <Grid>
        <!-- Hintergrund Steam Gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#171A21" Offset="0.0"/>
                <GradientStop Color="#171A21" Offset="0.5"/>
                <GradientStop Color="#171A21" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- SLIDE HOST -->
        <ScrollViewer Grid.Row="0"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <Grid x:Name="SlideContainer"
                  HorizontalAlignment="Center"
                  MaxWidth="800"
                  Padding="20"/>
        </ScrollViewer>

        <!-- Navigation Buttons -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Grid.Row="1"
                    Spacing="20"
                    Margin="0,10,0,20">
            <Button Content="Back" Click="BackButton_Click"/>
            <Button Content="Next" Click="NextButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
