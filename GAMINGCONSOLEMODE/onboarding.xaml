<Page
    x:Class="GAMINGCONSOLEMODE.Onboarding"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GAMINGCONSOLEMODE"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Storyboard x:Name="FadeInWebView">
            <DoubleAnimation
                Storyboard.TargetName="MyWebView"
                Storyboard.TargetProperty="Opacity"
                From="0.0"
                To="1.0"
                Duration="0:0:0.5" />
        </Storyboard>
    </Page.Resources>

    <Grid Background="#2A2A2A">

        <ProgressRing x:Name="LoadingSpinner"
                      IsActive="True"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Width="80"
                      Height="80" />

        <TextBlock x:Name="ErrorText"
                   Text="Fehler beim Laden der Roadmap. Bitte prüfe deine Internetverbindung."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="White"
                   FontSize="16"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   Margin="20"
                   Visibility="Collapsed" />
        <WebView2 x:Name="MyWebView" 
                  Source="https://www.gameconsolemode.com/onboarding"
                  Opacity="0" 
                  NavigationCompleted="MyWebView_NavigationCompleted" />

    </Grid>
</Page>