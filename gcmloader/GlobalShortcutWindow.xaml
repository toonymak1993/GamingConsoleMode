<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="gcmloader.BlankWindow1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Active Shortcuts">

    <Grid x:Name="RootBackground" Opacity="0" Loaded="RootBackground_Loaded">

        <Image x:Name="WallpaperImage" Stretch="UniformToFill" />

        <Border Background="#CC000000" />

        <Border x:Name="MainPopupCard" 
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="#E60A0A0A"
                BorderBrush="{ThemeResource SystemControlHighlightAccentBrush}"
                BorderThickness="1"
                CornerRadius="16"
                Padding="40"
                RenderTransformOrigin="0.5,0.5">

            <Border.RenderTransform>
                <CompositeTransform x:Name="PopupTransform" ScaleX="0.8" ScaleY="0.8" TranslateY="40"/>
            </Border.RenderTransform>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10" Margin="0,0,0,5">
                    <FontIcon Glyph="&#xE765;" FontSize="24" Foreground="White" />
                    <TextBlock Text="ACTIVE SHORTCUTS" FontSize="22" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="Press B / O or combo again to close" FontSize="12" Foreground="#FFAAAAAA" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden">
                    <ItemsControl x:Name="ShortcutItemsControl">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VariableSizedWrapGrid Orientation="Vertical" MaximumRowsOrColumns="8" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#1AFFFFFF" CornerRadius="12" Margin="6" Padding="20,0,20,0" Height="65" Width="320">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding KeysDisplay}" FontSize="15" FontWeight="SemiBold" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" VerticalAlignment="Center" TextWrapping="Wrap"/>

                                        <TextBlock Grid.Column="1" Text="{Binding ActionName}" FontSize="14" FontWeight="Normal" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>