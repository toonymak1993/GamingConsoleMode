name: Build and Create Installer

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number for the installer'
        required: false
        default: '1.0.0'
      build_configuration:
        description: 'Build configuration'
        required: true
        default: 'Release'
        type: choice
        options:
          - Debug
          - Release
      platform:
        description: 'Platform architecture'
        required: true
        default: 'x64'
        type: choice
        options:
          - x64
          - x86
          - ARM64
      create_release:
        description: 'Create GitHub release'
        required: true
        default: false
        type: boolean
      release_notes:
        description: 'Release notes (if creating release)'
        required: false
        default: 'Development build'


env:
  SOLUTION_PATH: './GAMINGCONSOLEMODE.sln'
  BUILD_PLATFORM: 'x64'
  DOTNET_VERSION: '8.0.x'
  WINDOWS_SDK_VERSION: '10.0.19041.0'
  INNO_SETUP_VERSION: '6.2.2'
  ISS_FILE: '.github/installer.iss'

jobs:
  build-and-package:
    runs-on: windows-latest
    
    outputs:
      version: ${{ steps.version.outputs.version }}
      artifact-name: ${{ steps.artifact-name.outputs.name }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check repository structure
        shell: pwsh
        run: |
          Write-Host "📂 Repository root contents:"
          Get-ChildItem -Path . | Select-Object Name, Mode | Format-Table
          
          Write-Host "`n📋 Looking for solution files:"
          $slnFiles = Get-ChildItem -Path . -Filter "*.sln" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 5
          if ($slnFiles) {
            foreach ($sln in $slnFiles) {
              Write-Host "  Found: $($sln.FullName.Replace($PWD, '.'))"
            }
          } else {
            Write-Warning "No .sln files found!"
          }
          
          Write-Host "`n📁 Looking for project files:"
          $projFiles = Get-ChildItem -Path . -Filter "*.csproj" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 10
          if ($projFiles) {
            foreach ($proj in $projFiles) {
              Write-Host "  Found: $($proj.FullName.Replace($PWD, '.'))"
            }
          } else {
            Write-Warning "No .csproj files found!"
          }

      - name: Determine version
        id: version
        shell: pwsh
        run: |
          $baseVersion = "${{ github.event.inputs.version }}"
          
          # Add build number for development builds
          if ("${{ github.event.inputs.create_release }}" -eq "false") {
            $version = "${baseVersion}-dev.${{ github.run_number }}"
          } else {
            $version = $baseVersion
          }
          
          echo "version=$version" >> $env:GITHUB_OUTPUT
          echo "🏷️ Building version: $version"
          echo "🔧 Configuration: ${{ github.event.inputs.build_configuration }}"
          echo "🖥️ Platform: ${{ github.event.inputs.platform }}"
          echo "📦 Create Release: ${{ github.event.inputs.create_release }}"

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install .NET Workloads
        shell: pwsh
        run: |
          # Install required workloads for WinUI3/Windows App SDK
          Write-Host "📦 Installing .NET workloads..."
          
          # Install Windows development workload
          dotnet workload install maui-windows
          
          Write-Host "✅ .NET workloads installed"

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Restore NuGet packages
        run: |
          dotnet restore ${{ env.SOLUTION_PATH }} `
            --verbosity normal

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build solution
        shell: pwsh
        run: |
          $platform = "${{ github.event.inputs.platform }}"
          
          # Build using MSBuild with platform-specific settings
          msbuild ${{ env.SOLUTION_PATH }} `
            /p:Configuration=${{ github.event.inputs.build_configuration }} `
            /p:Platform="$platform" `
            /p:Version=${{ steps.version.outputs.version }} `
            /p:AssemblyVersion=${{ github.event.inputs.version }}.0 `
            /p:FileVersion=${{ github.event.inputs.version }}.0 `
            /p:PublishReadyToRun=false `
            /p:PublishSingleFile=false `
            /p:WindowsPackageType=None `
            /p:WindowsAppSDKSelfContained=true `
            /restore `
            /m `
            /v:m
          
          if ($LASTEXITCODE -ne 0) {
            Write-Error "Build failed with exit code: $LASTEXITCODE"
            exit $LASTEXITCODE
          }

      - name: Run tests (optional)
        if: github.event.inputs.build_configuration == 'Release'
        continue-on-error: true
        run: |
          dotnet test ${{ env.SOLUTION_PATH }} `
            --configuration ${{ github.event.inputs.build_configuration }} `
            --no-build `
            --verbosity normal `
            --logger "trx;LogFileName=test-results.trx"

      - name: Prepare GCM build outputs
        shell: pwsh
        run: |
          # Create directory for installer files
          New-Item -ItemType Directory -Force -Path "./installer-files"
          
          $config = "${{ github.event.inputs.build_configuration }}"
          $platform = "${{ github.event.inputs.platform }}"
          Write-Host "📦 Collecting build outputs from $config configuration for $platform"
          
          # List of GCM components to include
          # WinUI3 apps with net8.0-windows10.0.19041.0 target
          $components = @{
            # Main applications - try multiple possible paths
            "GAMINGCONSOLEMODE" = @(
              "GAMINGCONSOLEMODE/bin/$platform/$config/net8.0-windows10.0.19041.0/*",
              "GAMINGCONSOLEMODE/bin/$config/net8.0-windows10.0.19041.0/*",
              "GAMINGCONSOLEMODE/bin/$platform/$config/net8.0-windows/*",
              "GAMINGCONSOLEMODE/bin/$platform/$config/*",
              "GAMINGCONSOLEMODE/bin/$config/*"
            )
            "gcmloader" = @(
              "gcmloader/bin/$platform/$config/net8.0-windows10.0.19041.0/*",
              "gcmloader/bin/$config/net8.0-windows10.0.19041.0/*",
              "gcmloader/bin/$platform/$config/net8.0-windows/*",
              "gcmloader/bin/$platform/$config/*",
              "gcmloader/bin/$config/*"
            )
            "wingamepad" = @(
              "wingamepad/bin/$platform/$config/net8.0-windows10.0.19041.0/*",
              "wingamepad/bin/$config/net8.0-windows10.0.19041.0/*",
              "wingamepad/bin/$platform/$config/net8.0-windows/*",
              "wingamepad/bin/$platform/$config/*",
              "wingamepad/bin/$config/*"
            )
            "Overlaywindow" = @(
              "Overlaywindow/bin/$platform/$config/net8.0-windows10.0.19041.0/*",
              "Overlaywindow/bin/$config/net8.0-windows10.0.19041.0/*",
              "Overlaywindow/bin/$platform/$config/net8.0-windows/*",
              "Overlaywindow/bin/$platform/$config/*",
              "Overlaywindow/bin/$config/*"
            )
            "flowlauncher" = @(
              "flowlauncher/bin/$platform/$config/net8.0-windows10.0.19041.0/*",
              "flowlauncher/bin/$config/net8.0-windows10.0.19041.0/*",
              "flowlauncher/bin/$platform/$config/net8.0-windows/*",
              "flowlauncher/bin/$platform/$config/*",
              "flowlauncher/bin/$config/*"
            )
          }
          
          $successCount = 0
          $failCount = 0
          
          foreach ($component in $components.GetEnumerator()) {
            $copied = $false
            $destPath = "./installer-files/$($component.Key)"
          
            foreach ($sourcePath in $component.Value) {
              $fullPath = "./$sourcePath"
              if (Test-Path $fullPath) {
                New-Item -ItemType Directory -Force -Path $destPath | Out-Null
                Copy-Item -Path $fullPath -Destination $destPath -Recurse -Force
                Write-Host "✅ Copied $($component.Key) files from: $sourcePath"
                $successCount++
                $copied = $true
                break
              }
            }
          
            if (-not $copied) {
              Write-Warning "❌ Could not find $($component.Key) in any expected location"
              Write-Warning "   Searched paths:"
              foreach ($path in $component.Value) {
                Write-Warning "   - $path"
              }
              $failCount++
            }
          }
          
          # Copy any additional resources or dependencies
          # Example: Copy logo.ico if it exists in the source
          $logoSource = "./gcmloader/logo.ico"
          if (Test-Path $logoSource) {
            Copy-Item -Path $logoSource -Destination "./installer-files/gcmloader/" -Force
            Write-Host "✅ Copied logo.ico"
          } else {
            # Try to find logo.ico in other locations
            $alternativeLogoPaths = @(
              "./logo.ico",
              "./Resources/logo.ico",
              "./gcmloader/Resources/logo.ico"
            )
            foreach ($path in $alternativeLogoPaths) {
              if (Test-Path $path) {
                New-Item -ItemType Directory -Force -Path "./installer-files/gcmloader/" | Out-Null
                Copy-Item -Path $path -Destination "./installer-files/gcmloader/logo.ico" -Force
                Write-Host "✅ Copied logo.ico from $path"
                break
              }
            }
          }
          
          # Summary
          Write-Host "`n📊 Build Collection Summary:"
          Write-Host "   ✅ Success: $successCount components"
          Write-Host "   ❌ Failed: $failCount components"
          
          # List contents for debugging
          Write-Host "`n📁 Installer files structure:"
          Get-ChildItem -Path "./installer-files" -Recurse -Directory | ForEach-Object {
            $relativePath = $_.FullName.Replace("$PWD\installer-files\", "").Replace("\", "/")
            Write-Host "  └─ $relativePath"
          }
          
          # Show total size
          $totalSize = (Get-ChildItem -Path "./installer-files" -Recurse -File | Measure-Object -Property Length -Sum).Sum / 1MB
          Write-Host "`n💾 Total size: $([math]::Round($totalSize, 2)) MB"
          
      - name: Download and install Inno Setup
        shell: pwsh
        run: |
          $innoVersion = "${{ env.INNO_SETUP_VERSION }}"
          $url = "https://files.jrsoftware.org/is/6/innosetup-$innoVersion.exe"
          $installerPath = "$env:TEMP\innosetup.exe"
          
          Write-Host "📥 Downloading Inno Setup $innoVersion..."
          Invoke-WebRequest -Uri $url -OutFile $installerPath
          
          Write-Host "📦 Installing Inno Setup..."
          Start-Process -FilePath $installerPath -ArgumentList "/VERYSILENT", "/SUPPRESSMSGBOXES", "/NORESTART", "/SP-" -Wait
          
          # Add Inno Setup to PATH
          $innoPath = "${env:ProgramFiles(x86)}\Inno Setup 6"
          echo "$innoPath" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          echo "INNO_PATH=$innoPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Update version in ISS file
        shell: pwsh
        run: |
          $issPath = "${{ env.ISS_FILE }}"
          $version = "${{ steps.version.outputs.version }}"
          
          if (Test-Path $issPath) {
            $content = Get-Content $issPath -Raw
          
            # Update the version define
            if ($content -match '#define MyAppVersion ".*"') {
              $content = $content -replace '#define MyAppVersion ".*"', "#define MyAppVersion `"$version`""
            } else {
              # If no define exists, add it at the beginning
              $content = "#define MyAppVersion `"$version`"`n" + $content
            }
          
            Set-Content -Path $issPath -Value $content -Encoding UTF8
            Write-Host "✅ Updated ISS file with version $version"
          
            # Display first few lines for verification
            Write-Host "`n📄 ISS file header:"
            Get-Content $issPath -First 5 | ForEach-Object { Write-Host "  $_" }
          } else {
            Write-Error "ISS file not found at: $issPath"
            exit 1
          }

      - name: Build installer with Inno Setup
        shell: cmd
        run: |
          echo 🔨 Building GCM installer...
          "%INNO_PATH%\ISCC.exe" /Q /O"./output" "${{ env.ISS_FILE }}"
          echo.
          echo ✅ Installer build complete!
          echo.
          echo 📦 Generated files:
          dir /B output\*.exe

      - name: Generate artifact name
        id: artifact-name
        shell: pwsh
        run: |
          $shortSha = "${{ github.sha }}".Substring(0, 7)
          $config = "${{ github.event.inputs.build_configuration }}".ToLower()
          $name = "GCM-installer-${{ steps.version.outputs.version }}-$config-$shortSha"
          echo "name=$name" >> $env:GITHUB_OUTPUT
          Write-Host "📦 Artifact name: $name"

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.artifact-name.outputs.name }}
          path: ./output/GCM-Setup-*.exe
          retention-days: 30

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ github.event.inputs.build_configuration }}-${{ github.sha }}
          path: |
            **/*.log
            **/*.trx
          retention-days: 7

      - name: Build Summary
        if: always()
        shell: pwsh
        run: |
          Write-Host "`n" 
          Write-Host "========================================" 
          Write-Host "         BUILD SUMMARY" 
          Write-Host "========================================" 
          Write-Host "Version:        ${{ steps.version.outputs.version }}"
          Write-Host "Configuration:  ${{ github.event.inputs.build_configuration }}"
          Write-Host "Artifact:       ${{ steps.artifact-name.outputs.name }}"
          Write-Host "Build Number:   ${{ github.run_number }}"
          Write-Host "Commit:         ${{ github.sha }}"
          Write-Host "========================================" 
          
#  release:
#    needs: build-and-package
#    runs-on: ubuntu-latest
#    if: startsWith(github.ref, 'refs/tags/v')
#    
#    permissions:
#      contents: write
#    
#    steps:
#      - name: Download installer artifact
#        uses: actions/download-artifact@v4
#        with:
#          name: ${{ needs.build-and-package.outputs.artifact-name }}
#          path: ./installer
#
#      - name: Create Release
#        uses: softprops/action-gh-release@v1
#        with:
#          tag_name: ${{ github.ref_name }}
#          name: Release ${{ needs.build-and-package.outputs.version }}
#          draft: false
#          prerelease: ${{ contains(needs.build-and-package.outputs.version, '-beta') || contains(needs.build-and-package.outputs.version, '-alpha') }}
#          files: ./installer/Setup-*.exe
#          body: |
#            ## Release ${{ needs.build-and-package.outputs.version }}
#            
#            ### Changes
#            - Built from commit: ${{ github.sha }}
#            - Build number: ${{ github.run_number }}
#            
#            ### Installation
#            Download and run the installer below.
#            
#            ### Requirements
#            - Windows 10 or later
#            - .NET Runtime ${{ env.DOTNET_VERSION }}
#          generate_release_notes: true